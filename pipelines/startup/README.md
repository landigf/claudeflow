# Startup autopilot skeletons

- This preset is intentionally template-first. It gives you recurring audit/report skeletons without introducing a scheduler yet.
- Start reviews on `cheap` (gpt-5-mini) and escalate targeted follow-up runs to `deep`.
- Use `local` (auto) for privacy-sensitive drafts or low-cost iteration.
